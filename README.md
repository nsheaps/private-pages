A git/GitHub client that uses GitHub app/oauth app auth to load and serve static pages from private repos as if they were served as GitHub pages

also includes reusable GitHub actions to deploy privately built pages, potentially from a public repo. a good use case for this maybe something like preview environments for client only github pages apps or private docs requiring org membersip to view
